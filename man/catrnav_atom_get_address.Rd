% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atom_ad.R
\encoding{UTF-8}
\name{catrnav_atom_get_address}
\alias{catrnav_atom_get_address}
\title{ATOM INSPIRE: Download all the cadastral addresses of a municipality}
\usage{
catrnav_atom_get_address(
  munic,
  cache = TRUE,
  update_cache = FALSE,
  cache_dir = NULL,
  verbose = FALSE
)
}
\arguments{
\item{munic}{Municipality to extract. It can be a part of a string or the
cadastral code. See \link{catrnav_atom_get_address_db_all}.}

\item{cache}{A logical value indicating whether to use caching. Default is
\code{TRUE}. See \strong{About caching} section on \code{\link[=catrnav_set_cache_dir]{catrnav_set_cache_dir()}}.}

\item{update_cache}{A logical value indicating whether to update the cache.
Default is \code{FALSE}. When set to \code{TRUE}, forces a fresh download of the
source file.}

\item{cache_dir}{A path to a cache directory. On missing value the function
stores the cached files in a temporary dir (see \code{\link[base:tempdir]{base::tempdir()}}).}

\item{verbose}{Logical, displays information. Useful for debugging,
default is \code{FALSE}.}
}
\value{
A \code{\link[sf:st_sf]{sf}} object.
}
\description{
Get the spatial data of all the cadastral addresses belonging to a single
municipality using the INSPIRE ATOM service.
}
\examples{
\donttest{

s <- catrnav_atom_get_address("Tudela")

library(ggplot2)

ggplot(s) +
  geom_sf() +
  labs(
    title = "Addresses",
    subtitle = "Tudela"
  )
}
}
\references{
\href{https://geoportal.navarra.es/es/inspire}{SITNA – Catastro de Navarra}
}
\seealso{
Other ATOM:
\code{\link[=catrnav_atom_get_address_db_all]{catrnav_atom_get_address_db_all()}},
\code{\link[=catrnav_atom_get_buildings]{catrnav_atom_get_buildings()}},
\code{\link[=catrnav_atom_get_buildings_db_all]{catrnav_atom_get_buildings_db_all()}},
\code{\link[=catrnav_atom_get_parcels]{catrnav_atom_get_parcels()}},
\code{\link[=catrnav_atom_get_parcels_db_all]{catrnav_atom_get_parcels_db_all()}}

Other addresses:
\code{\link[=catrnav_atom_get_address_db_all]{catrnav_atom_get_address_db_all()}}
}
\concept{ATOM}
\concept{addresses}
