% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache-utils.R
\encoding{UTF-8}
\name{catrnav_clear_cache}
\alias{catrnav_clear_cache}
\title{Clear your \pkg{CatastRoNav} cache directory}
\usage{
catrnav_clear_cache(config = FALSE, cached_data = TRUE, verbose = FALSE)
}
\arguments{
\item{config}{A logical value indicating whether to delete the
\pkg{CatastRoNav} configuration directory.}

\item{cached_data}{If \code{TRUE}, deletes your \code{cache_dir} and all its contents.}

\item{verbose}{Logical. If \code{TRUE}, displays informational messages.}
}
\value{
\code{NULL}, invisibly. This function is called for its side effects.
}
\description{
Use this function with caution. It clears cached data and configuration,
specifically:
\itemize{
\item Deletes the \pkg{CatastRoNav} configuration directory
(\code{tools::R_user_dir("CatastRoNav", "config")}).
\item Deletes the \code{cache_dir} directory.
\item Clears the \code{CATASTRONAV_CACHE_DIR} environment variable.
}
}
\details{
This function resets the cache state as if you had never used
\pkg{CatastRoNav}.
}
\examples{

# Caution! This modifies your current state.
\dontrun{
my_cache <- catrnav_detect_cache_dir()

example_cache <- file.path(tempdir(), "example", "cache")
catrnav_set_cache_dir(example_cache, verbose = FALSE)

catrnav_clear_cache(verbose = TRUE)

# Restore the initial cache.
catrnav_set_cache_dir(my_cache)
identical(my_cache, catrnav_detect_cache_dir())
}
}
\seealso{
Manage the local cache:
\code{\link[=catrnav_set_cache_dir]{catrnav_set_cache_dir()}}
}
\concept{cache_utilities}
