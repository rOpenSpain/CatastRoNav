% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wfs-bu.R
\encoding{UTF-8}
\name{catrnav_wfs_get_buildings_bbox}
\alias{catrnav_wfs_get_buildings_bbox}
\title{WFS INSPIRE: download buildings}
\usage{
catrnav_wfs_get_buildings_bbox(x, srs = 4326, verbose = FALSE, count = NULL)
}
\arguments{
\item{x}{See \strong{Bounding box}. Can be one of:
\itemize{
\item A numeric vector of length 4 with the coordinates that define
the bounding box: \code{c(xmin, ymin, xmax, ymax)}.
\item A \code{sf/sfc} object, as provided by the \href{https://CRAN.R-project.org/package=sf}{\pkg{sf}} package.
}}

\item{srs}{SRS/CRS to use in the query. Defaults to \code{4326}. See
\strong{Bounding box}.}

\item{verbose}{Logical. If \code{TRUE}, displays informational messages.}

\item{count}{Positive whole number specifying the maximum number of features
to return. If \code{NULL}, the service default applies.}
}
\value{
An \code{\link[sf:st_sf]{sf}} object. Returns \code{NULL} if the data cannot be
retrieved.
}
\description{
Retrieve spatial building data from the Cadastre of Navarre WFS INSPIRE
service. \code{catrnav_wfs_get_buildings_bbox()} retrieves objects included in
the provided bounding box. See \strong{Bounding box}.
}
\section{API limits}{

The service returns a maximum of 5,000 features by default. Use \code{count} to
request a smaller result.
}

\section{Bounding box}{

When \code{x} is a numeric vector, make sure that \code{srs} matches the coordinate
values. The function queries the bounding box in
\href{https://epsg.io/25830}{EPSG:25830}, ETRS89 / UTM zone 30N, then transforms
the result back to \code{srs}.

When \code{x} is an \code{\link[sf:st_sf]{sf}} or \code{sfc} object, \code{srs} is ignored. The
object's bounding box is used for the query and the result is transformed
back to the input CRS. See \code{\link[sf:st_bbox]{sf::st_bbox()}}.
}

\examples{
\dontshow{if (run_example()) withAutoprint(\{ # examplesIf}
downtown <- c(-1.646812, 42.814528, -1.638036, 42.820320)

bu <- catrnav_wfs_get_buildings_bbox(downtown, srs = 4326)

library(ggplot2)

ggplot(bu) +
  geom_sf()
\dontshow{\}) # examplesIf}
}
\references{
\href{https://geoportal.navarra.es/es/inspire}{SITNA – Catastro de Navarra}
}
\seealso{
Query data from WFS INSPIRE services:
\code{\link[=catrnav_wfs_get_address_bbox]{catrnav_wfs_get_address_bbox()}},
\code{\link[=catrnav_wfs_get_parcels_bbox]{catrnav_wfs_get_parcels_bbox()}}

Work with cadastral buildings:
\code{\link[=catrnav_atom_get_buildings]{catrnav_atom_get_buildings()}},
\code{\link[=catrnav_atom_get_buildings_db_all]{catrnav_atom_get_buildings_db_all()}}
}
\concept{buildings}
\concept{wfs}
